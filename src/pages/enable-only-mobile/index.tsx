import styles from './styles.module.scss';

export default function EnableOnlyMobile() {
  
  return (
    <main className={styles.enableOnlyMobile}>
      <h1>A aplicação está disponível apenas para smartphones.</h1>
    </main>
  )
}
